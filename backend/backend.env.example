# Flask backend environment variables

# Change this to a strong password before deploying!
ADMIN_PASSWORD=admin1234

# Flask settings
FLASK_ENV=development
FLASK_DEBUG=True

# Database path (relative to backend folder)
DB_PATH=contacts.db

# Allowed frontend origin for CORS (change to your deployed frontend URL)
FRONTEND_URL=http://localhost:3000
